# Ableton Hub - Dependencies
# Install with: pip install -r requirements.txt

# UI Framework
PyQt6>=6.6.0

# Database
SQLAlchemy>=2.0.0
aiosqlite>=0.19.0

# File System Monitoring
watchdog>=3.0.0

# Network Discovery (Ableton Link)
zeroconf>=0.131.0

# Async Qt Integration
qasync>=0.27.0

# Image Processing (for artwork)
Pillow>=10.0.0

# Fuzzy String Matching (for export detection)
rapidfuzz>=3.0.0

# ML/AI Features (Phase 5+) - Lazy loaded to avoid slow startup
scikit-learn>=1.3.0  # Used in: ml_clustering, similarity_analyzer, ml_feature_extractor
numpy>=1.24.0  # Used in: ml_feature_extractor, similarity_analyzer, ml_clustering, recommendation_engine
pandas>=2.0.0  # Future: Statistics & Analytics Dashboard (timeline analysis, data aggregation, export)

# Audio Feature Extraction (for ML content analysis) - Lazy loaded
librosa>=0.10.0  # Used in: ml_feature_extractor (audio analysis)
soundfile>=0.12.0  # Used in: ml_feature_extractor (required by librosa)

# Enhanced XML Parsing (for deeper ALS analysis) - Optional, falls back to stdlib
lxml>=4.9.0  # Used in: als_parser (optional, improves XPath support)

# Optional: Enhanced ALS parsing
# pyableton>=0.1.0  # Uncomment if available
# abletonparsing>=0.1.0  # For .asd clip files (Live 9/10/11)

# Development Dependencies (optional)
# pytest>=7.4.0
# pytest-qt>=4.2.0
# pytest-asyncio>=0.21.0
# black>=23.0.0
# ruff>=0.1.0
# mypy>=1.5.0
